# ===========================================
# BACKEND CONFIGURATION - Admin API
# ===========================================

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (SHARED DATABASE)
MONGO_URI=mongodb://127.0.0.1:27017/siupo

# Default Admin Account (Auto-created on startup if not exists)
# Email: cnpmm@admin.com
# Password: Admin@123456
# ⚠️ IMPORTANT: Change password after first login!

# JWT Configuration
JWT_SECRET=yR3pF7tW9qZx2Vn8mC0sL4dA6eH1kU5j
JWT_ACCESS_EXPIRATION=1h
JWT_REFRESH_EXPIRATION=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Email/SMTP Configuration (for OTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
MAIL_USER=hcl2k4@gmail.com
MAIL_PASS=ljpq dvqb ibvt guhs
FROM_EMAIL=hcl2k4@gmail.com
FROM_NAME=CNPMM Admin

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
